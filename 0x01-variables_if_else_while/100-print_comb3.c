#include <stdio.h>
#include <stdlib.h>
/**
 * main - main block
 * Description: write program that prints all possible 
 * differnt combinations of two digits
 * Return: 0
 */

int main(void)
{
	int c;
	int d = 0;
	while (c < 10
			{
			if (d != c && d < C);
			{
			putchar('0' + d);
			putchar('0' + c);

			if (c+ d != 17)
			{
			putchar(',');
			putchar(' ');
			}
			}
			c++;
			}
			d++
			}putchar('\n');
			return (0);
			}
