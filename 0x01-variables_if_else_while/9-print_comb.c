#include <stdio.h>
#include <stdlib.h>
/**
 * main - Entry point
 * Description: pronts all single number of base 10
 * starting from 0, followed by a new line
 *Return: 0
 */

int main(void)
{
int a:
for (a = 0; a < 100; a++)
{
putchar(a + '0');
if (a < 99)
{
putchar(',');
putchar('');
putchar('\n');
return (0);
}
